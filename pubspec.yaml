name: axtp_flutter
description: Dart and Flutter runtime for AXTP core, broker, endpoint, and dynamic RPC.
version: 0.0.2
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
